dest = join_paths(get_option('datadir'), 'icons', 'MokshaMoonLight')
 
install_subdir(join_paths(meson.current_source_dir(), 'MokshaMoonlight'), 
                install_dir: dest,
                strip_directory: true
              )
